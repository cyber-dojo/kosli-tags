name: Main - Kosli tags

on:
  push:
    branches:
      - main

env:
  KOSLI_ORG:       ${{ vars.KOSLI_ORG }}               # cyber-dojo
  KOSLI_HOST:      ${{ vars.KOSLI_HOST }}              # https://app.kosli.com
  KOSLI_API_TOKEN: ${{ secrets.KOSLI_API_TOKEN }}
  KOSLI_DRY_RUN:   ${{ vars.KOSLI_DRY_RUN }}           # false
  KOSLI_DEBUG:     ${{ vars.KOSLI_DEBUG }}             # false
  KOSLI_AWS_PROD:  aws-prod

jobs:

  apply-tags:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Kosli CLI
        uses: kosli-dev/setup-cli-action@v2
        with:
          version: ${{ vars.KOSLI_CLI_VERSION }}

      - name: Tag Kosli Flows
        run: |
          kosli tag flow custom-start-points-ci \
            --set kind=build \
            --set ci=github \
            --set repo_url="${{ github.server_url }}/${{ github.repository_owner }}/custom-start-points"
          
          kosli tag flow dashboard-ci \
            --set kind=build \
            --set ci=github \
            --set repo_url="${{ github.server_url }}/${{ github.repository_owner }}/dashboard"
          
          kosli tag flow differ-ci \
            --set kind=build \
            --set ci=github \
            --set repo_url="${{ github.server_url }}/${{ github.repository_owner }}/differ"
          
          kosli tag flow exercises-start-points-ci \
            --set kind=build \
            --set ci=github \
            --set repo_url="${{ github.server_url }}/${{ github.repository_owner }}/exercises-start-points"
          
          kosli tag flow languages-start-points-ci \
            --set kind=build \
            --set ci=github \
            --set repo_url="${{ github.server_url }}/${{ github.repository_owner }}/languages-start-points"
          
          kosli tag flow nginx-ci \
            --set kind=build \
            --set ci=github \
            --set repo_url="${{ github.server_url }}/${{ github.repository_owner }}/nginx"
          
          kosli tag flow runner-ci \
            --set kind=build \
            --set ci=github \
            --set repo_url="${{ github.server_url }}/${{ github.repository_owner }}/runner"
          
          kosli tag flow saver-ci \
            --set kind=build \
            --set ci=github \
            --set repo_url="${{ github.server_url }}/${{ github.repository_owner }}/saver"
          
          kosli tag flow web-ci \
            --set kind=build \
            --set ci=github \
            --set repo_url="${{ github.server_url }}/${{ github.repository_owner }}/web"
          
          kosli tag flow creator-ci \
            --set kind=build \
            --set ci=gitlab \
            --set repo_url=https://gitlab.com/cyber-dojo/creator
